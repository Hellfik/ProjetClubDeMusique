{% extends 'adminBase.html.twig'%} {% block body %} {% for message in app.flashes('success') %}
<div class="container">
    <div class="col-md-12 alert alert-success">
        {{ message }}
    </div>
</div>
{% endfor %}
<div class="card col-md-8 mx-auto my-5 px-0">
    <div class="card-header bg-success">
        <div class="card-header bg-success">
            <h2>Gérer les Instruments</h2>
        </div>
    </div>
    <div class="card-body">
        <h5 class="card-title text-center">La liste complète des instruments</h5>
        <div class="card-text table-responsive">
            <table id="dtBasicExample" class="table table-striped table-bordered" cellspacing="0">
                <thead>
                    <tr class="text-center">
                        <th class="th-sm">#</th>
                        <th class="th-sm">Nom
                        </th>
                        <th class="th-sm">Description
                        </th>
                        <th class="th-sm">Famille d'instrument
                        </th>
                        <th class="th-sm">Image
                        </th>
                        <th colspan="2" class="th-sm">Actions
                        </th>
                    </tr>
                </thead>
                <tbody>
                    {% set compteur = 0 %} {% for instrument in instruments %}
                    <tr class="text-center h-100">
                        {% set compteur = compteur + 1 %}
                        <td class="align-middle">{{ compteur }}</td>
                        <td class="align-middle">{{instrument.nom}}</td>
                        <td class="align-middle">{{instrument.description | slice(0,20)}} ...</td>
                        <td class="align-middle">{{instrument.familleInstruments.nom}}
                        </td>
                        <td class="align-middle">{{instrument.imageName}}
                        </td>
                        <td colspan="2"><a href="{{path('admin_edit_instrument', {'id' : instrument.id})}}" class="btn btn-success m-2">Modifier</a><a href="{{path('admin_delete_instrument', {'id' : instrument.id})}}" class="btn btn-danger m-2">Supprimer</a></td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        <ul>
    </div>
</div>
{% endblock %}