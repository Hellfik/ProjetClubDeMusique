{% extends 'base.html.twig' %} {% block stylesheets %}
<link rel="stylesheet" href="{{asset('css/edm.css')}}"> {% endblock %} {% block body %}
<div class="col-12 margin">
    <h2 class="card-title h1 text-white">Ajouter un instrument</h2>
</div>
<section class="description col-md-12">
    {{form_start(formInstrument)}} {{form_widget(formInstrument)}}
    <button type="submit" class="btn btn-success">Ajouter un instrument</button> {{form_end(formInstrument)}}
</section>
{% endblock %}